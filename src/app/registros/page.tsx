import { PlaceholderScreen } from "@/components/placeholder-screen";
import { FileText } from "lucide-react";

export default function RegistrosPage() {
  return (
    <PlaceholderScreen
      icon={FileText}
      title="Registros"
      description="Anotações de saída de reunião, organizadas por categoria."
      sprintLabel="Sprint 2 constrói esta tela"
    />
  );
}
